# dependencies
node_modules/

# generated eval runs (reports, scores). never commit
eval/runs/

# local connector config written by the panel wizard. per-machine, not shared
designmd-loop.config.json

# secrets and env. never commit
.env
.env.*
*.local

# OS / editor cruft
.DS_Store
.fuse_hidden*
*.swp
.idea/
.vscode/
