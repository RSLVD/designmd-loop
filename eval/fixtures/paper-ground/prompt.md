Generate a single self-contained HTML snippet for a Meridian hero section:
an eyebrow, a large display headline, a one-line standfirst, and a scroll cue.
Inline all styles. No external dependencies.

Follow DESIGN.md: warm paper ground (never pure black or pure white), Inter display
type set large and bold in ink, generous whitespace, IBM Plex Mono for the eyebrow,
and the accent reserved for at most a single small element. No glows or gradients.
